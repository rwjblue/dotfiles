#!/usr/bin/env bash
# -*- mode: sh; sh-shell: bash; -*-
#MISE description="Fetch latest changes and create new branch from trunk"
#MISE depends=["jj:fetch"]
#MISE dir="{{cwd}}"

set -euo pipefail

exec jj new 'trunk()'
